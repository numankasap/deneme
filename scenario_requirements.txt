# Scenario Bot v2.0 Requirements
# Senaryo görsel oluşturma + metin sadeleştirme + kalite kontrolü

# Supabase
supabase>=2.0.0

# Google Gemini (yeni paket)
google-genai>=1.0.0

# HTML to PNG
playwright>=1.40.0

# Resim işleme
Pillow>=10.0.0

# JSON işleme (standart kütüphane, ekstra gerek yok)
# Logging (standart kütüphane)
# String Template (standart kütüphane)
