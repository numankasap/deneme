name: Advanced Crypto Analysis Bot

on:
  schedule:
    # Her saat başı çalışır
    - cron: '0 * * * *'
  workflow_dispatch:
    # Manuel çalıştırma butonu (test için)
    inputs:
      debug_mode:
        description: 'Debug modu aktif mi?'
        required: false
        default: 'false'

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # 500 coin taraması için artırıldı

    steps:
      - name: 📥 Depoyu Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'  # Bağımlılıkları cache'le

      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🚀 Botu Çalıştır
        env:
          # Zorunlu API Anahtarları
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          
          # Opsiyonel API Anahtarı (Ücretsiz)
          CRYPTOPANIC_API: ${{ secrets.CRYPTOPANIC_API }}
          # Not: Blockchair, CoinGecko, DeFiLlama API key gerektirmez
        run: python bot.py

      - name: 📊 Çalışma Özeti
        if: always()
        run: |
          echo "🕐 Çalışma Zamanı: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "✅ Bot başarıyla tamamlandı!"
