name: BIST 100 Analiz Botu

on:
  schedule:
    - cron: '30 6 * * 1-5'   # 09:30 TR (Piyasa açılışı) - Hafta içi
  workflow_dispatch:
    inputs:
      stock_count:
        description: 'Analiz edilecek hisse sayısı'
        required: false
        default: '30'

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: 📥 Depoyu Çek
        uses: actions/checkout@v4

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 📦 Kütüphaneleri Yükle
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📈 BIST Analizi Çalıştır
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python bist_bot.py

      - name: 📊 Çalışma Özeti
        if: always()
        run: |
          echo "🕐 Çalışma: $(TZ='Europe/Istanbul' date '+%Y-%m-%d %H:%M:%S')"
          echo "✅ BIST analizi tamamlandı!"
