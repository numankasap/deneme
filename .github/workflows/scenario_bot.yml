name: Scenario Image Bot v5 - Hybrid

on:
  schedule:
    # Her gün saat 03:00 ve 15:00'te çalışır (UTC)
    - cron: '0 3,15 * * *'
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'İşlenecek soru sayısı'
        required: false
        default: '20'
      test_mode:
        description: 'Test modu (true/false)'
        required: false
        default: 'false'

env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
  GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

jobs:
  generate-visuals:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install google-genai supabase Pillow

      - name: Run Image Bot
        env:
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '20' }}
          TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
        run: |
          python scenario_bot.py

      - name: Summary
        if: always()
        run: |
          echo "## 🎨 Senaryo Görsel Botu v5.0 - Hybrid Tamamlandı" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Kullanılan Modeller:" >> $GITHUB_STEP_SUMMARY
          echo "- **Imagen 4 Standard**: Grafikler, tablolar, sayı doğrusu ($0.04/görsel)" >> $GITHUB_STEP_SUMMARY
          echo "- **Imagen 4 Ultra**: 3D, geometri, mimari ($0.06/görsel)" >> $GITHUB_STEP_SUMMARY
          echo "- **Gemini 3 Pro Image**: Metin ağırlıklı kartlar ($0.134/görsel)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Ayarlar:" >> $GITHUB_STEP_SUMMARY
          echo "- Batch Size: ${{ github.event.inputs.batch_size || '20' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Test Mode: ${{ github.event.inputs.test_mode || 'false' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Yenilikler v5.0:" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Geometri soruları artık destekleniyor" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Çözüm görselleştirmesi eklendi" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Akıllı model seçimi" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 3D ve perspektif çizimleri için Ultra" >> $GITHUB_STEP_SUMMARY
