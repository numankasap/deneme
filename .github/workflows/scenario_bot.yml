# Scenario Bot Workflow
# PISA ve senaryo tabanlı sorular için infografik görsel üretir
# Günde 3 kez çalışır (09:00, 15:00, 21:00 UTC+3)
# Her seansta 30 soru işler, test modunda 10 soru

name: Scenario Bot

on:
  # Manuel tetikleme
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Test modu (10 soru)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      batch_size:
        description: 'İşlenecek soru sayısı (1-100)'
        required: false
        default: '30'
        type: string
  
  # Zamanlanmış çalışma (Geometri botundan 1 saat sonra)
  schedule:
    # 06:00 UTC = 09:00 TR
    - cron: '0 6 * * *'
    # 12:00 UTC = 15:00 TR
    - cron: '0 12 * * *'
    # 18:00 UTC = 21:00 TR
    - cron: '0 18 * * *'

jobs:
  generate-scenario-visuals:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scenario_requirements.txt
      
      - name: 🎭 Install Playwright browsers
        run: |
          playwright install chromium
          playwright install-deps chromium
      
      - name: 🖼️ Run Scenario Bot
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
          BATCH_SIZE: ${{ github.event.inputs.batch_size || '30' }}
        run: |
          echo "🚀 Senaryo Görsel Botu Başlatılıyor..."
          echo "📅 Tarih: $(date)"
          echo "🔧 Test Modu: $TEST_MODE"
          echo "📊 Batch Boyutu: $BATCH_SIZE soru"
          python scenario_bot.py
      
      - name: 📊 Job Summary
        if: always()
        run: |
          echo "## 🖼️ Senaryo Bot Çalışması Tamamlandı" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Tarih:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "- **Test Modu:** ${{ github.event.inputs.test_mode || 'false' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Batch Boyutu:** ${{ github.event.inputs.batch_size || '30' }} soru" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Detaylar için logları kontrol edin." >> $GITHUB_STEP_SUMMARY
